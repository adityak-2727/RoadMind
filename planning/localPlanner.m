function candidateTrajectories = localPlanner(egoState, globalPath, predictedTrajectories, plannerConfig)
% localPlanner - stub: will sample a set of candidate short-horizon
% trajectories (e.g. lattice/frenet sampling) around the global path.
% Phase 0: no logic yet.
%
% Inputs:
%   egoState              - current ego state struct (see config/createEgoState.m)
%   globalPath             - Nx2 reference waypoints from globalPlanner
%   predictedTrajectories  - cell array of predicted agent trajectories
%   plannerConfig          - struct from config/plannerConfig.m
% Output:
%   candidateTrajectories - cell array of Nx2 candidate waypoint arrays

candidateTrajectories = {};

end
