function predictedTrajectory = irregularMotionModel(agent, horizon, dt)
% irregularMotionModel - stub: will model erratic/unpredictable motion for
% classes like pedestrian, animal, pushcart, bicycle on unstructured roads
% (e.g. widened uncertainty cone instead of a single deterministic path).
% Phase 0: no logic yet.
%
% Inputs:
%   agent   - single agent struct (see config/createAgent.m)
%   horizon - [s] prediction horizon
%   dt      - [s] prediction timestep
% Output:
%   predictedTrajectory - Nx2 array of [x, y] waypoints (placeholder empty)

predictedTrajectory = zeros(0, 2);

end
