function predictedTrajectory = constantVelocityPrediction(agent, horizon, dt)
% constantVelocityPrediction - stub: will project an agent forward assuming
% constant velocity/heading. Appropriate for cars/buses/trucks on structured
% motion. Phase 0: no logic yet.
%
% Inputs:
%   agent   - single agent struct (see config/createAgent.m)
%   horizon - [s] prediction horizon
%   dt      - [s] prediction timestep
% Output:
%   predictedTrajectory - Nx2 array of [x, y] waypoints (placeholder empty)

predictedTrajectory = zeros(0, 2);

end
